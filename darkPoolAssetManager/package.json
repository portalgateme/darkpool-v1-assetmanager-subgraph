{
  "name": "darkPoolAssetManager",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined singularity/darkPoolAssetManager",
    "create-local": "graph create --node http://localhost:8020/ singularity/darkPoolAssetManager",
    "remove-local": "graph remove --node http://localhost:8020/ singularity/darkPoolAssetManager",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 singularity/darkPoolAssetManager",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.1",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
